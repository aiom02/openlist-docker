# 快速开始：部署 OpenList 到 70.39.205.183

## 🚀 最快速的部署方式（推荐）

### Windows 用户

**方法 1：使用官方 Docker 镜像（最简单）**

1. 双击运行 `杂项\快速部署_Docker版.bat`
2. 选择选项 `1`
3. 输入服务器密码
4. 等待部署完成
5. 浏览器访问: http://70.39.205.183:5244

**方法 2：使用自定义前端**

1. 双击运行 `杂项\快速部署_Docker版.bat`
2. 选择选项 `2`
3. 输入服务器密码
4. 等待构建和上传完成
5. 浏览器访问: http://70.39.205.183:5244

### Linux/Mac 用户

```bash
# 在项目根目录执行
bash 杂项/快速部署_Docker版.sh
```

---

## 📝 默认登录信息

- **访问地址**: http://70.39.205.183:5244
- **用户名**: admin
- **密码**: admin

⚠️ **首次登录后请立即修改密码！**

---

## 🔄 后续更新前端

当你修改了前端代码后，要更新到服务器：

### Windows 用户
双击运行 `杂项\更新前端到服务器.bat`

### Linux/Mac 用户
```bash
# 1. 在本地构建前端
cd OpenList-Frontend-main
pnpm build
cd ..

# 2. 上传到服务器
scp -r OpenList-Frontend-main/dist/* root@70.39.205.183:/opt/openlist/public/dist/

# 3. 重启服务
ssh root@70.39.205.183 "docker restart openlist"
```

---

## 🛠️ 常用管理命令

### 通过 SSH 连接服务器

```bash
ssh root@70.39.205.183
```

### 查看服务状态

```bash
docker ps
```

### 查看日志

```bash
docker logs -f openlist
```

### 重启服务

```bash
docker restart openlist
```

### 停止服务

```bash
docker stop openlist
```

### 启动服务

```bash
docker start openlist
```

### 更新到最新版本

```bash
cd /opt/openlist
docker-compose pull
docker-compose up -d
```

---

## 📦 数据备份

```bash
# 备份数据
ssh root@70.39.205.183
cd /opt/openlist
tar -czf openlist-backup-$(date +%Y%m%d).tar.gz data

# 下载到本地
scp root@70.39.205.183:/opt/openlist/openlist-backup-*.tar.gz ./
```

---

## 🔧 故障排查

### 1. 无法访问服务

```bash
# SSH 连接到服务器
ssh root@70.39.205.183

# 检查服务是否运行
docker ps | grep openlist

# 检查端口
netstat -tlnp | grep 5244

# 检查防火墙
iptables -L -n | grep 5244

# 如果需要开放端口
iptables -A INPUT -p tcp --dport 5244 -j ACCEPT
```

### 2. 前端显示不正常

```bash
# 清除浏览器缓存
# 按 Ctrl+Shift+Delete (Windows) 或 Cmd+Shift+Delete (Mac)

# 或者使用硬刷新
# 按 Ctrl+F5 (Windows) 或 Cmd+Shift+R (Mac)
```

### 3. 查看详细日志

```bash
ssh root@70.39.205.183
docker logs --tail 100 openlist
```

---

## 🌐 配置域名访问（可选）

如果你有域名，可以配置 Nginx 反向代理：

```bash
# SSH 连接到服务器
ssh root@70.39.205.183

# 安装 Nginx
apt-get update
apt-get install -y nginx

# 创建配置文件
cat > /etc/nginx/sites-available/openlist << 'EOF'
server {
    listen 80;
    server_name 你的域名.com;

    client_max_body_size 1G;

    location / {
        proxy_pass http://localhost:5244;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
EOF

# 启用配置
ln -s /etc/nginx/sites-available/openlist /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

然后就可以通过域名访问了！

---

## 📚 更多信息

- **详细部署文档**: 查看 `部署到服务器指南.md`
- **OpenList 官方文档**: https://doc.oplist.org.cn
- **问题反馈**: https://github.com/OpenListTeam/OpenList/issues
- **Telegram 群组**: https://t.me/OpenListTeam

---

## ✅ 部署检查清单

- [ ] 服务器可以 SSH 连接
- [ ] 服务器已安装 Docker（脚本会自动安装）
- [ ] 服务器端口 5244 已开放
- [ ] 可以访问 http://70.39.205.183:5244
- [ ] 已使用默认账号密码登录
- [ ] 已修改默认密码
- [ ] 已测试文件上传下载功能
- [ ] 已配置存储（本地或云存储）

---

## 🎉 完成！

如果一切顺利，现在你应该可以访问 http://70.39.205.183:5244 并看到 OpenList 的界面了！

祝使用愉快！如有问题，请查看详细的部署文档或联系技术支持。

