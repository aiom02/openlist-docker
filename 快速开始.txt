┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃          OpenList Docker 部署快速指南                  ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

📦 本地构建步骤 (Windows)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣ 构建前端
   双击运行: 1-构建前端.bat
   
2️⃣ 构建 Docker 镜像
   双击运行: 2-构建Docker镜像.bat
   
3️⃣ 导出镜像文件
   双击运行: 3-导出Docker镜像.bat
   
   ✅ 将在 docker-images-export 目录生成以下文件:
      - openlist-backend.tar
      - openlist-frontend.tar
      - docker-compose.yml
      - 服务器部署脚本.sh (可选)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 服务器部署步骤 (Linux)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

方法一: 使用自动部署脚本 (推荐)
─────────────────────────────────────────────────────

1. 上传文件到服务器
   scp -r docker-images-export/* user@your-server:/opt/openlist/

2. SSH 连接服务器
   ssh user@your-server

3. 进入目录并运行部署脚本
   cd /opt/openlist
   bash 服务器部署脚本.sh

   完成！脚本会自动完成所有部署步骤。


方法二: 手动部署
─────────────────────────────────────────────────────

1. 上传文件
   scp -r docker-images-export/* user@your-server:/opt/openlist/

2. SSH 连接
   ssh user@your-server
   cd /opt/openlist

3. 加载镜像
   docker load -i openlist-backend.tar
   docker load -i openlist-frontend.tar

4. 创建数据目录
   mkdir -p /opt/openlist/data
   chown -R 1001:1001 /opt/openlist/data
   chmod 755 /opt/openlist/data

5. 启动服务
   docker-compose up -d

6. 查看状态
   docker-compose ps
   docker-compose logs -f

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔧 常用管理命令
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

查看日志:         docker-compose logs -f
重启服务:         docker-compose restart
停止服务:         docker-compose stop
停止并删除容器:   docker-compose down
查看容器状态:     docker-compose ps
进入容器:         docker exec -it openlist-backend sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 访问地址
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

前端:          http://your-server-ip:80
后端 API:      http://your-server-ip:5244
WebDAV:        http://your-server-ip:5245

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔐 生产环境建议
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 使用 Nginx/Caddy 反向代理
2. 配置 SSL 证书 (Let's Encrypt)
3. 配置防火墙 (只开放 80/443 端口)
4. 定期备份数据目录
5. 设置自动重启策略

详细说明请查看: 部署说明.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

