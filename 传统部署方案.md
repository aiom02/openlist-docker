# OpenList 传统部署方案（非Docker）

除了 Docker 部署，OpenList 项目还支持以下几种传统部署方式。根据你的服务器环境和需求选择合适的方案。

---

## 📦 方案一：直接编译部署（推荐）

这是最简单、资源占用最小的方式，适合所有 Linux 服务器。

### 1. 在本地编译

#### 编译后端
```bash
# 进入后端目录
cd OpenList-main

# 为 Linux 编译（在 Windows 上交叉编译）
$env:GOOS="linux"
$env:GOARCH="amd64"
go build -o openlist main.go

# 或者在 Linux 服务器上直接编译
# go build -o openlist main.go
```

#### 构建前端
```bash
# 进入前端目录
cd OpenList-Frontend-main

# 安装依赖
pnpm install

# 构建生产版本
pnpm run build
# 构建完成后会生成 dist 目录
```

### 2. 上传到服务器

```bash
# 创建服务器目录
mkdir -p /opt/openlist
cd /opt/openlist

# 上传文件（在本地执行）
# 上传后端可执行文件
scp OpenList-main/openlist user@your-server:/opt/openlist/

# 上传前端构建文件
scp -r OpenList-Frontend-main/dist user@your-server:/opt/openlist/public/

# 创建数据目录
mkdir -p /opt/openlist/data
```

### 3. 配置后端

```bash
# 在服务器上操作
cd /opt/openlist

# 赋予执行权限
chmod +x openlist

# 创建配置文件 config.json
cat > data/config.json << 'EOF'
{
  "force": false,
  "address": "0.0.0.0",
  "port": 5244,
  "assets": "./public",
  "database": {
    "type": "sqlite3",
    "host": "",
    "port": 0,
    "user": "",
    "password": "",
    "name": "",
    "db_file": "data/data.db",
    "table_prefix": "x_",
    "ssl_mode": "",
    "dsn": ""
  },
  "scheme": {
    "https": false,
    "cert_file": "",
    "key_file": ""
  },
  "temp_dir": "data/temp",
  "log": {
    "enable": true,
    "name": "data/log/log.log",
    "max_size": 50,
    "max_backups": 30,
    "max_age": 28,
    "compress": false
  }
}
EOF
```

### 4. 使用 systemd 管理服务（推荐）

创建系统服务配置：

```bash
# 创建服务文件
sudo nano /etc/systemd/system/openlist.service
```

写入以下内容：

```ini
[Unit]
Description=OpenList Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/openlist
ExecStart=/opt/openlist/openlist server
Restart=on-failure
RestartSec=5s

# 资源限制
LimitNOFILE=65536

# 日志
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
# 重新加载 systemd
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start openlist

# 设置开机自启
sudo systemctl enable openlist

# 查看服务状态
sudo systemctl status openlist

# 查看日志
sudo journalctl -u openlist -f
```

---

## 🔄 方案二：使用 Supervisor 管理（适合多进程管理）

如果你的服务器上运行多个服务，Supervisor 是个很好的选择。

### 1. 安装 Supervisor

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install supervisor

# CentOS/RHEL
sudo yum install supervisor
```

### 2. 配置 OpenList 服务

```bash
# 创建配置文件
sudo nano /etc/supervisor/conf.d/openlist.conf
```

写入以下内容：

```ini
[program:openlist]
command=/opt/openlist/openlist server
directory=/opt/openlist
autostart=true
autorestart=true
startretries=3
user=root
redirect_stderr=true
stdout_logfile=/var/log/openlist/access.log
stderr_logfile=/var/log/openlist/error.log
environment=PATH="/usr/bin:/usr/local/bin"

[program:openlist-backend]
command=/opt/openlist/openlist server
directory=/opt/openlist
autostart=true
autorestart=true
user=root
```

### 3. 管理服务

```bash
# 创建日志目录
sudo mkdir -p /var/log/openlist

# 更新配置
sudo supervisorctl reread
sudo supervisorctl update

# 启动服务
sudo supervisorctl start openlist

# 查看状态
sudo supervisorctl status

# 重启服务
sudo supervisorctl restart openlist

# 查看日志
sudo tail -f /var/log/openlist/access.log
```

---

## 🌐 方案三：使用 Nginx 反向代理（推荐用于生产环境）

使用 Nginx 作为反向代理可以提供 HTTPS、负载均衡、静态文件服务等功能。

### 1. 安装 Nginx

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nginx

# CentOS/RHEL
sudo yum install nginx
```

### 2. 配置 Nginx

```bash
# 创建配置文件
sudo nano /etc/nginx/sites-available/openlist
```

#### 方案 A: Nginx 提供前端静态文件（推荐）

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 修改为你的域名或IP
    
    # 前端静态文件
    location / {
        root /opt/openlist/public/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
        
        # 静态文件缓存
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
    
    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:5244;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # DAV 支持
    location /dav/ {
        proxy_pass http://127.0.0.1:5244;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # DAV 方法支持
        proxy_request_buffering off;
        client_max_body_size 0;
    }
    
    # 文件上传大小限制
    client_max_body_size 10G;
}
```

#### 方案 B: Nginx 纯代理模式

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 全部流量代理到后端
    location / {
        proxy_pass http://127.0.0.1:5244;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 缓冲设置
        proxy_buffering off;
        proxy_request_buffering off;
        
        # 超时
        proxy_connect_timeout 60s;
        proxy_send_timeout 3600s;
        proxy_read_timeout 3600s;
    }
    
    client_max_body_size 10G;
}
```

### 3. 启用 HTTPS（推荐）

使用 Let's Encrypt 免费证书：

```bash
# 安装 certbot
sudo apt install certbot python3-certbot-nginx

# 获取证书并自动配置 Nginx
sudo certbot --nginx -d your-domain.com

# 证书会自动续期，测试续期
sudo certbot renew --dry-run
```

或者手动配置 HTTPS：

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    # SSL 优化
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    # ... 其他配置同上 ...
}

# HTTP 重定向到 HTTPS
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}
```

### 4. 启用配置

```bash
# 启用站点
sudo ln -s /etc/nginx/sites-available/openlist /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重新加载
sudo systemctl reload nginx
```

---

## 🚀 方案四：使用 Caddy（自动 HTTPS）

Caddy 是一个现代的 Web 服务器，自动管理 HTTPS 证书。

### 1. 安装 Caddy

```bash
# Ubuntu/Debian
sudo apt install -y debian-keyring debian-archive-keyring apt-transport-https
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' | sudo gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt' | sudo tee /etc/apt/sources.list.d/caddy-stable.list
sudo apt update
sudo apt install caddy
```

### 2. 配置 Caddyfile

```bash
sudo nano /etc/caddy/Caddyfile
```

写入配置：

```caddy
# 自动 HTTPS
your-domain.com {
    # 前端静态文件
    root * /opt/openlist/public/dist
    file_server
    
    # API 反向代理
    handle /api/* {
        reverse_proxy localhost:5244
    }
    
    handle /dav/* {
        reverse_proxy localhost:5244
    }
    
    # SPA 路由支持
    try_files {path} /index.html
}

# 或者纯代理模式
# your-domain.com {
#     reverse_proxy localhost:5244
# }
```

### 3. 启动 Caddy

```bash
# 重新加载配置
sudo systemctl reload caddy

# 查看状态
sudo systemctl status caddy

# 查看日志
sudo journalctl -u caddy -f
```

---

## 📊 方案五：使用 PM2（Node.js 环境）

如果需要在开发模式下运行前端，可以使用 PM2。

### 1. 安装 PM2

```bash
npm install -g pm2
# 或
pnpm add -g pm2
```

### 2. 后端配置

创建 `ecosystem.config.js`：

```javascript
module.exports = {
  apps: [
    {
      name: 'openlist-backend',
      script: './openlist',
      args: 'server',
      cwd: '/opt/openlist',
      instances: 1,
      autorestart: true,
      watch: false,
      max_memory_restart: '1G',
      env: {
        NODE_ENV: 'production'
      }
    }
  ]
}
```

### 3. 启动服务

```bash
# 启动
pm2 start ecosystem.config.js

# 查看状态
pm2 status

# 查看日志
pm2 logs openlist-backend

# 设置开机自启
pm2 startup
pm2 save
```

---

## 🔧 方案六：云平台部署

### 阿里云/腾讯云 ECS

1. **购买服务器**：选择合适的 Linux 实例（推荐 Ubuntu 20.04+）
2. **配置安全组**：开放 80、443、5244 端口
3. **按方案一部署**：使用 systemd + Nginx
4. **配置域名**：在云平台添加 DNS 解析

### 宝塔面板部署

1. **安装宝塔**：
```bash
# Ubuntu/Debian
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh
```

2. **通过宝塔管理**：
   - 在「网站」中添加静态站点，指向 `/opt/openlist/public/dist`
   - 在「软件商店」安装 Nginx
   - 配置反向代理到 5244 端口
   - 在「终端」中启动 OpenList 后端

### Vercel/Netlify（仅前端）

如果只想部署前端，后端另外部署：

```bash
# 修改前端 API 地址
# 在 OpenList-Frontend-main/.env.production 中
VITE_API_URL=https://your-backend-api.com

# 构建
pnpm run build

# 上传 dist 目录到 Vercel/Netlify
```

---

## 📝 部署后的管理

### 查看日志

```bash
# systemd 服务
sudo journalctl -u openlist -f

# Supervisor
sudo tail -f /var/log/openlist/access.log

# Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# OpenList 应用日志
tail -f /opt/openlist/data/log/log.log
```

### 数据备份

```bash
# 创建备份脚本
cat > /opt/openlist/backup.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/opt/openlist-backups"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR

# 备份数据库和配置
tar -czf $BACKUP_DIR/openlist-data-$DATE.tar.gz \
    /opt/openlist/data/data.db \
    /opt/openlist/data/config.json

# 只保留最近 7 天的备份
find $BACKUP_DIR -name "openlist-data-*.tar.gz" -mtime +7 -delete

echo "Backup completed: $BACKUP_DIR/openlist-data-$DATE.tar.gz"
EOF

chmod +x /opt/openlist/backup.sh

# 添加定时任务（每天凌晨 3 点备份）
crontab -e
# 添加：0 3 * * * /opt/openlist/backup.sh
```

### 更新部署

```bash
# 1. 停止服务
sudo systemctl stop openlist

# 2. 备份数据
cp -r /opt/openlist/data /opt/openlist/data.backup

# 3. 上传新的可执行文件
scp openlist user@your-server:/opt/openlist/

# 4. 上传新的前端文件
scp -r dist/* user@your-server:/opt/openlist/public/

# 5. 启动服务
sudo systemctl start openlist
```

---

## 🔐 安全加固建议

### 1. 防火墙配置

```bash
# 使用 ufw
sudo ufw allow 22/tcp     # SSH
sudo ufw allow 80/tcp     # HTTP
sudo ufw allow 443/tcp    # HTTPS
sudo ufw enable

# 使用 firewalld
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

### 2. 禁止直接访问后端端口

```bash
# 只允许本地访问 5244 端口
sudo ufw deny 5244/tcp
# 或在配置文件中将 address 改为 127.0.0.1
```

### 3. 设置文件权限

```bash
# 设置合理的文件权限
sudo chown -R www-data:www-data /opt/openlist
sudo chmod -R 755 /opt/openlist
sudo chmod 600 /opt/openlist/data/config.json
```

### 4. 定期更新

```bash
# 系统更新
sudo apt update && sudo apt upgrade -y

# 定期更新 OpenList
```

---

## 🎯 各方案对比

| 方案 | 难度 | 资源占用 | 适用场景 | 推荐指数 |
|------|------|----------|----------|----------|
| systemd + Nginx | ⭐⭐ | 低 | 生产环境 | ⭐⭐⭐⭐⭐ |
| Supervisor | ⭐⭐ | 低 | 多服务管理 | ⭐⭐⭐⭐ |
| Caddy | ⭐ | 低 | 快速部署 | ⭐⭐⭐⭐⭐ |
| PM2 | ⭐⭐ | 中 | Node.js 环境 | ⭐⭐⭐ |
| 宝塔面板 | ⭐ | 中 | 新手友好 | ⭐⭐⭐⭐ |
| Docker | ⭐ | 中 | 容器化 | ⭐⭐⭐⭐⭐ |

---

## ❓ 常见问题

### Q: 哪种方案最适合我？

- **新手推荐**：宝塔面板 或 Caddy
- **生产环境**：systemd + Nginx + HTTPS
- **多服务器**：Docker
- **低资源服务器**：直接编译 + systemd

### Q: 如何选择数据库？

OpenList 默认使用 SQLite，足够日常使用。如需高并发，可在 `config.json` 中配置 MySQL/PostgreSQL。

### Q: 前端必须用 Nginx 吗？

不是。Go 后端可以直接提供静态文件服务。使用 Nginx 主要是为了：
- 更好的静态文件性能
- HTTPS 支持
- 负载均衡
- 缓存控制

### Q: 端口被占用怎么办？

```bash
# 查看端口占用
sudo netstat -tulpn | grep 5244

# 修改配置文件中的端口
nano /opt/openlist/data/config.json
# 修改 "port": 5244 为其他端口
```

---

## 📞 获取帮助

- 查看项目文档：`README.md`
- 查看日志排查问题
- GitHub Issues: https://github.com/OpenListTeam/OpenList/issues

---

**部署成功后访问**: `http://your-server-ip` 或 `https://your-domain.com` 🎉

